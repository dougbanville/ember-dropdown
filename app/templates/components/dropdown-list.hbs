{{yield (hash 
  item=(component "dropdown-list-item" 
  onMouseEnter=(action "setActiveItem") 
  onMouseLeave=(action "clearActiveItem")
  activeItem=activeItem
  )
)}}
<p>The active Item is {{ activeItem }}</p>